# BitChat Environment Variables Example
# Copy this file to .env and fill in your values

# Apple Developer Team ID (required for code signing)
DEVELOPMENT_TEAM=YOUR_TEAM_ID_HERE

# Optional: Custom bundle identifier suffix
# BUNDLE_ID_SUFFIX=.dev

# Optional: Build configuration
# BUILD_CONFIGURATION=Debug

# Note: This app doesn't require API keys as it uses local Bluetooth mesh networking
# No server endpoints or external services are needed
